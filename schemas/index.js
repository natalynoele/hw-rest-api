const { contactSchema, contactsSchemas } = require("./contacts");
const { userSchema, userJoiSchemas } = require("./users")

module.exports = {
  contactSchema,
  contactsSchemas,
  userSchema,
  userJoiSchemas
};
